// Elements
const input = document.getElementById("userInput");
const sendBtn = document.getElementById("sendBtn");
const chatArea = document.getElementById("chatArea");

// Message history for the AI
let messages = [
  {
    role: "system",
    content: `
You are Nova AI.
Created and owned by itzlootdev.

You are NOT ChatGPT.
You are NOT developed by OpenAI.

Your Development Name is NovaCreations.

Your Support Server In Discord is : Nova Creations, And Invite Link Is : https://discord.gg/kxyFtrh9Ya

Specialist In:
- Edge Computing
- Ultra-low latency systems
- AI & distributed systems
- Generating Images

Main CEO Of You:
 Afsal
 Ahzan 
 Ammar

Always introduce yourself as Nova AI at the start of a new conversation.
When the user asks for an image, respond ONLY with the image URL or base64 (no extra text unless requested).
    `
  }
];

// Add message to chat (supports text, single image, or mixed array)
function addMessage(content, type) {
  const div = document.createElement("div");
  div.className = `message ${type}`;

  // Handle different content types
  if (typeof content === "string") {
    // If it's an image (URL or base64)
    if (content.startsWith("http") || content.startsWith("data:image")) {
      const img = document.createElement("img");
      img.src = content;
      img.alt = "Generated by Nova AI";
      img.loading = "lazy";
      img.style.maxWidth = "100%";
      img.style.borderRadius = "12px";
      img.style.marginTop = "8px";
      img.style.boxShadow = "0 4px 12px rgba(0,0,0,0.5)";
      div.appendChild(img);
    } else {
      // Normal text
      div.innerText = content;
    }
  } 
  else if (Array.isArray(content)) {
    // Multiple parts: text + images mixed
    content.forEach(part => {
      if (typeof part === "string") {
        if (part.startsWith("http") || part.startsWith("data:image")) {
          const img = document.createElement("img");
          img.src = part;
          img.alt = "Generated by Nova AI";
          img.loading = "lazy";
          img.style.maxWidth = "100%";
          img.style.borderRadius = "12px";
          img.style.marginTop = "8px";
          img.style.display = "block";
          div.appendChild(img);
        } else {
          const textNode = document.createTextNode(part);
          div.appendChild(textNode);
          div.appendChild(document.createElement("br"));
        }
      }
    });
  } 
  else {
    div.innerText = String(content);
  }

  chatArea.appendChild(div);
  chatArea.scrollTop = chatArea.scrollHeight;
}

// Send message on button or Enter (not Shift+Enter)
sendBtn.onclick = sendMessage;
input.addEventListener("keydown", e => {
  if (e.key === "Enter" && !e.shiftKey) {
    e.preventDefault();
    sendMessage();
  }
});

async function sendMessage() {
  const text = input.value.trim();
  if (!text) return;

  addMessage(text, "user");
  input.value = "";

  messages.push({ role: "user", content: text });

  try {
    const res = await fetch("/api/chat", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ messages })
    });

    if (!res.ok) throw new Error("Bad response");

    const data = await res.json();

    // data.reply can be: string (text or image url/base64) OR array (mixed)
    addMessage(data.reply, "bot");

    // Save assistant reply to history
    messages.push({ role: "assistant", content: data.reply });

  } catch (err) {
    console.error(err);
    addMessage("Error connecting to Nova AI. Try again later.", "bot");
  }
}

// Auto-focus input when page loads
window.onload = () => input.focus();